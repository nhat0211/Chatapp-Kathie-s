version: '3.8'
services:
  app:
    image: chatapp-kathie
    build:
      context: ./
      dockerfile: dockerfile
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
        
    ports:
      - "3001:3001"
    restart: unless-stopped